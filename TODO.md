the p test is DEFINITELY two sided. im getting for NEGATIVE vuong_z a p < 0.01

try GERPT instead

try their fine-tune methodology instead

extend the test set to 0.1 OR filter the datasets - something is wrong with biology surprisal

the delta log likelihood is wrt no surprisal model??
